# Lombok configurations
# Properties documented here: https://projectlombok.org/features/configuration

# Enforce that only this config file must apply
config.stopBubbling = true

# We want to exclude Lombok generated code from code coverage
lombok.addLombokGeneratedAnnotation = true

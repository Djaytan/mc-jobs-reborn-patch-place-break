# This is a test config file

type = "SQLITE"
table = "patch_place_break_tag"
# Another comment
dbmsServer = {
    host = {
        hostname = "localhost"
        port = 3306
        isSslEnabled = true
    }
    credentials = {
        username = "username"
        password = "password"
    }
    database = "database"
}
connectionPool = {
    # MISSING FIELDS HERE
}

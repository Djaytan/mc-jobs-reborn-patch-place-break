# This is a test config file

dataSource:
  # One comment
  type: 'SQLITE'
  table: 'patch_place_break_tag'
  # Another comment
  dbmsServer:
    host:
      hostname: 'localhost'
      port: 3306
      isSslEnabled: true
    credentials:
      username: 'username'
      password: 'password'
    database: 'database'
  connectionPool:
    # Yet another comment
    connectionTimeout: 30000
    poolSize: 10
